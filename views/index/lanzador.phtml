<?php
// se definen las variables de session
$ssn_id_cliente = $_SESSION["usuario"]["id_cliente"];
$ssn_cliente = $_SESSION["usuario"]["nombre_cliente"];
$ssn_id_usuario = $_SESSION["usuario"]["id_usuario"];
$ssn_id_perfil = $_SESSION["usuario"]["id_perfil"];
$user_log = $_SESSION["usuario"]["user_log"];
$password = $_SESSION["usuario"]["pass"];
$vista = '';
//se realiza el metodo para la consulta 
$array = $this->lanzador->getLanzador($ssn_id_perfil);

if ($array) {
    $tamano = count($array);
    if ($tamano <= 2) {
        echo $vista = '
		<!-- Tarjetas-->
		<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 miestilo">
			<div class="contenedor">
				<div class="container-card">
					<div class="card" style="background-color:#1C2331;opacity:0.7;border-radius:10px;height:10%">
						<div class="contenido-card">
							<div class="title-cards">
								<h4>Catálogo de aplicaciones </h4>
							</div>
							<hr style="width:100%;background-color:yellow;height:1px;margin-top:1px">				   
		';
        foreach ($array as $key => $value) {
            $n = 0;

            if ($value['nombre'] == 'Operaciones') {
                $n = "fa-solid fa-truck-moving";
                $id_n = $value['id'];
            }

            if ($value['nombre'] == 'Parámetros') {
                $n = "fa-solid fa-rectangle-list";
                $id_p = $value['id'];
            }

            if ($value['nombre'] == 'Internacional') {
                $n = "fa-solid fa-plane-up";
                $id_i = $value['id'];
            }

            if ($value['nombre'] == 'Servicio al cliente') {
                $n = "fa-solid fa-handshake";
                $id_sc = $value['id'];
            }

            if ($value['nombre'] == 'Cumplimiento') {
                $n = "fa-solid fa-file-circle-check";
                $id_cu = $value['id'];
            }

            if ($value['nombre'] == 'Seguridad') {
                $n = "fa-solid fa-lock";
                $id_se = $value['id'];
            }

            if ($value['nombre'] == 'Tráfico') {
                $n = "fa-solid fa-traffic-light";
                $id_t = $value['id'];
            }

            if ($value['nombre'] == 'Almacenamiento') {
                $n = "fa-solid fa-warehouse";
                $id_a = $value['id'];
            }

            if ($value['nombre'] == 'Gerencia') {
                $n = "fa-solid fa-user-tie";
                $id_g = $value['id'];
            }

            if ($value['nombre'] == 'Trackign') {
                $n = "fa-solid fa-truck-fast";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Novedades') {
                $n = "fa-solid fa-spinner";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Informes') {
                $n = "fa-regular fa-file";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Bitacoras') {
                $n = "fa-solid fa-file-contract";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Torre de Control') {
                $n = "fa-solid fa-tower-observation";
                $id_cl = $value['id'];
            }
            // <a href="#" class="links lanzadorn cajas" data-id="' . encrypt($value['id']) . '" data-nombre="' . $value['nombre'] . '" data-cliente="' . $value['CLIENTE_ID'] . '" data-user_log="' . $user_log . '" data-password="' . $password . '" >
            echo $vista = '
			<div class="col-xs-12 col-md-2">
				<div class="card menus" style="background-color:#37474F;border: 0px solid yellow;border-radius:10px;">
					<a href="#" class="links lanzadorn cajas" data-id="' . $value['id'] . '" data-nombre="' . $value['nombre'] . '" data-cliente="' . $value['CLIENTE_ID'] . '" data-user_log="' . $user_log . '" data-password="' . $password . '" >
						<div class="contenido-card">
							<h5>' . $value['nombre'] . '</h5>
							<h4 class="text-center">
								<i class="' . $n . '" style="font-size:12pt; color:yellow;"></i>
								<hr style="width:100%;margin-top:0px;background-color:white;height:1px;margin-top:5px;text">
							</h4>
							<input type="hidden" id="' . $value['id'] . '" name="">
							
						</div>
					</a>
				</div>
			</div>';
        }
    } else {
        echo $vista = '
		<!-- Tarjetas-->
		<div class="col-xs-12 col-sm-12 col-md-12 col-lg-12 miestilo">
			<div class="contenedor">
				<div class="container-card">
					<div class="card" style="background-color:#1C2331;opacity:0.7;border-radius:10px;height:10%">
						<div class="contenido-card">
							<div class="title-cards">
								<h4>Catálogo de aplicaciones </h4>
							</div>
							<hr style="width:100%;background-color:yellow;height:1px;margin-top:1px">				   
		';
        foreach ($array as $key => $value) {
            $n = 0;

            // if ($value['nombre'] == 'Clientes') {
            //     $n = "fa-solid fa-users";
            //     $id_cl = $value['id'];
            // }

            if ($value['nombre'] == 'Operaciones') {
                $n = "fa-solid fa-truck-moving";
                $id_n = $value['id'];
            }

            if ($value['nombre'] == 'Parámetros') {
                $n = "fa-solid fa-rectangle-list";
                $id_p = $value['id'];
            }

            if ($value['nombre'] == 'Internacional') {
                $n = "fa-solid fa-plane-up";
                $id_i = $value['id'];
            }

            if ($value['nombre'] == 'Servicio al cliente') {
                $n = "fa-solid fa-handshake";
                $id_sc = $value['id'];
            }

            if ($value['nombre'] == 'Cumplimiento') {
                $n = "fa-solid fa-file-circle-check";
                $id_cu = $value['id'];
            }

            if ($value['nombre'] == 'Seguridad') {
                $n = "fa-solid fa-lock";
                $id_se = $value['id'];
            }

            if ($value['nombre'] == 'Tráfico') {
                $n = "fa-solid fa-traffic-light";
                $id_t = $value['id'];
            }

            if ($value['nombre'] == 'Almacenamiento') {
                $n = "fa-solid fa-warehouse";
                $id_a = $value['id'];
            }

            if ($value['nombre'] == 'Gerencia') {
                $n = "fa-solid fa-user-tie";
                $id_g = $value['id'];
            }

            if ($value['nombre'] == 'Trackign') {
                $n = "fa-solid fa-truck-fast";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Novedades') {
                $n = "fa-solid fa-spinner";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Informes') {
                $n = "fa-regular fa-file";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Bitacoras') {
                $n = "fa-solid fa-file-contract";
                $id_cl = $value['id'];
            }

            if ($value['nombre'] == 'Torre de Control') {
                $n = "fa-solid fa-tower-observation";
                $id_cl = $value['id'];
            }

            echo $vista = '
					<div class="col-xs-12 col-md-2">
						<div class="card menus" style="background-color:#37474F;border: 0px solid yellow;border-radius:10px;">
							<a href="#" class="links lanzadorn cajas" data-id="' . $value['id'] . '" data-nombre="' . $value['nombre'] . '" data-cliente="' . $value['CLIENTE_ID'] . '" data-user_log="' . $user_log . '" data-password="' . $password . '">
								<div class="contenido-card">
									<h5>' . $value['nombre'] . '</h5>
									<h4 class="text-center">
										<i class="' . $n . '" style="font-size:12pt; color:yellow;"></i>
										<hr style="width:100%;margin-top:0px;background-color:white;height:1px;margin-top:5px;text">
									</h4>
									<input type="hidden" id="' . $value['id'] . '" name="">
									
								</div>
							</a>
						</div>
					</div>';
        }
    }
}

$vista = ' 
				</div>
			</div>
		</div>
	</div>
</div>
<!--Fin   Tarjetas-->
';
/**** CARGA DE ARCHIVOS JS DE LA VISTA ****/
$vista .= '
		<!-- INICIO JS DEL BEAGLE -->
		<script src="%root_file%lib/jquery/jquery.min.js" type="text/javascript"></script>
		<script src="%root_file%lib/perfect-scrollbar/js/perfect-scrollbar.jquery.min.js" type="text/javascript"></script>
		<script src="%root_file%js/main.js" type="text/javascript"></script>
		<script src="%root_file%lib/bootstrap/dist/js/bootstrap.min.js" type="text/javascript"></script>

		<script type="text/javascript">
			$(document).ready(function(){
				//initialize the javascript
				App.init();
			});
		</script>
		<!-- FIN JS DEL BEAGLE -->
	';

/**** SE ASIGNAN LOS ELEMENTOS DE LA PAGINA  ****/
$vista = str_replace("%root_file%", $_layoutParams["ruta_layout"], $vista);
echo $vista;
?>
<script type="text/javascript">
    $(document).ready(function() {
        var valores = "";
        $(".lanzadorn").click(async function() {
            var ruta = ($(this).data("id"));
            var clientes = ($(this).data("nombre"));
            var cliente_id = ($(this).data("cliente"));
            var user_log = ($(this).data("user_log"));
            var password = ($(this).data("password"));
            // Validacion para redigir a los diferentes dashboard del sistema
            if (clientes === "Clientes") {
                // var url = 'http://127.0.0.1:8000/dashcliente/' + codificarBase64(cliente_id) + '/' + codificarBase64(user_log) + '/' + codificarBase64(password);
                // alert(url);
                var url = 'http://localhost/mvcLuisMiguel/index/index1/?idmenu=' + ruta;
                window.location = (url);
            } else {
                var url = 'http://localhost/mvcLuisMiguel/index/index1/?idmenu=' + ruta;
                window.location = (url);
            }
        });

        function codificarBase64(texto) {
            return btoa(texto);
        }

        // Función para decodificar Base64
        function decodificarBase64(textoCodificado) {
            return atob(textoCodificado);
        }
    });
</script>